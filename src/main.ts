// Minimal worker entry for the new plugin (no-op for now)
// Ensures build to build/main.js without affecting current manifest behavior

// eslint-disable-next-line @typescript-eslint/no-empty-function
figma.on('run', () => {});


